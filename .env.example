# Copy to .env.local and fill in. .env.local is git-ignored.

# DeepSeek API key — powers the "Ask Saatvik" chat agent (app/api/chat).
# Get one at https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=
